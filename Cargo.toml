[package]
name = "obols"
description = "A Standard Library for Black Charon"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "*", default-features = false, features = ["derive"], optional = true }

[features]
serde = ["dep:serde"]